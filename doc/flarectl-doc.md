# Flarectl Documentation

This document describes the command-line interface and functionality of the legacy `flarectl` tool.
We will analyze `ref/flarectl` to populate this.

## Commands

*To be populated*
